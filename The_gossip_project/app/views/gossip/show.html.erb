<div class="container mt-5">
  <h1 class="row justify-content-center">Gossip de <%= @gossip.author %></h1>
</div>

<div class="container">
  <div class="row">
    <div class="card col-5">
      <div class="card-body">
        <h5 class="card-title">Ecrit par <%= @gossip.author %></h5>
        <p class="card-text"><%= @gossip.content %></p>
        <p class="card-text">Ecrit le <%= @gossip.created_at.strftime('%d/%m/%Y') %> à <%= @gossip.created_at.strftime('%H:%M:%S') %>
      </div>
    </div>
  </div>
  <div class="row mt-3">
    <%= link_to 'Retour', '/' %>
  </div>
</div>
